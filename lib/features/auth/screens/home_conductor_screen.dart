import 'package:flutter/material.dart';
import 'package:py55/features/map/screens/driver_availability_screen.dart';

/// Pantalla principal para el rol Conductor / Mototaxista autenticado.
class HomeConductorScreen extends StatelessWidget {
  const HomeConductorScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const DriverAvailabilityScreen();
  }
}
