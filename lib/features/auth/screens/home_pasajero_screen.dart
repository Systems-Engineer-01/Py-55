import 'package:flutter/material.dart';
import 'package:py55/features/map/screens/passenger_map_screen.dart';

/// Pantalla principal para el rol Pasajero autenticado.
class HomePasajeroScreen extends StatelessWidget {
  const HomePasajeroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const PassengerMapScreen();
  }
}
